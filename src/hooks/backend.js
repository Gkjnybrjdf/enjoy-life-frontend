const protocol = 'http'
const host = 'localhost'
const port = 8080

const url = `${protocol}://${host}:${port}`

export default {
    protocol: protocol,
    host: host,
    port: port,
    url: url
}